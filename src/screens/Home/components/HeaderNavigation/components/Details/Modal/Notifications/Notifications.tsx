import React, { FC } from "react";
import * as Styles from "./Notifications.Style";

interface Props {}

const Notifications: FC = ({}) => {
  return <Styles.Container>Notifications</Styles.Container>;
};

export default Notifications;
